title = "KampusAbla gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Allow public client-side backend publishable key variables"
regexTarget = "line"
regexes = [
  '''VITE_SUPABASE_PUBLISHABLE_KEY\s*=\s*["']?[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+["']?''',
]
